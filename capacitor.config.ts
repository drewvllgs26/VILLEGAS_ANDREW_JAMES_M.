import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'act5_villegas',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
