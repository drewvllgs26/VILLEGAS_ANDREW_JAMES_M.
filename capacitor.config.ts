import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'threefold-act3',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
